input {
  tcp {
    port => 5044
    codec => json_lines
  }
}

filter {
  # Parse fields if needed. This is a minimal example.
  # You can add grok or mutate filters here to structure logs.
}

output {
  elasticsearch {
    hosts => ["http://elasticsearch:9200"]
    index => "microservicio-logs-%{+YYYY.MM.dd}"
    # Disable SSL/credentials for local practice
    # user => "elastic"
    # password => "changeme"
  }
  stdout { codec => rubydebug }
}
